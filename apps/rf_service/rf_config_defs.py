# rf_reader_v3/config.py
# -*- coding: utf-8 -*-
"""
v3 오프라인 판독 파이프라인 공통 설정.

좌표계: 공정 프로그램이 저장하는 이미지는 이미 cv2.rotate(ROTATE_90_CLOCKWISE)로
세로(1080x1920)로 회전된 상태다. 모든 좌표는 그 세로 이미지를 기준으로 한다.

핵심 설계
---------
- 카메라 고정 → 패널 원근보정 호모그래피와 ROI는 한 번 보정해 계속 재사용.
- CH1/CH2는 기울기가 반대라 단일 호모그래피로 못 펴므로 패널 3개를 개별 보정.
- 모든 인식은 빨강(R) 채널로 한다: LED는 빨강, 저장 이미지의 초록 진단 오버레이는
  R 채널에서 사라진다(검증 완료) → 오염된 raw 이미지도 학습/파싱에 사용 가능.
"""
from __future__ import annotations

# 패널별 원근보정: 원본(세로) 픽셀 4꼭짓점(TL,TR,BR,BL) → 펼친 평면 크기
PANELS = {
    "CH1": dict(src=[[112, 400], [975, 425], [978, 805], [110, 788]], size=(900, 400)),
    "RF3": dict(src=[[110, 915], [985, 935], [985, 1195], [110, 1180]], size=(900, 300)),
    "CH2": dict(src=[[110, 1310], [980, 1280], [982, 1600], [112, 1630]], size=(900, 330)),
}

# 칸별 ROI (원본 세로 좌표, [y0,y1,x0,x1]) — 디스플레이 숫자 블록을 감싼다(부호 제외).
ROIS = {
    "CH1_FWD":  [571, 610, 191, 291], "CH1_REF":  [586, 628, 405, 496],
    "CH1_LOAD": [604, 642, 630, 715], "CH1_TUNE": [611, 650, 794, 886],
    "RF3_LOAD": [997, 1042, 277, 413], "RF3_TUNE": [994, 1038, 465, 605],
    "CH2_FWD":  [1372, 1417, 203, 295], "CH2_REF":  [1348, 1394, 384, 499],
    "CH2_LOAD": [1332, 1375, 598, 714], "CH2_TUNE": [1312, 1350, 775, 878],
}

PANEL_OF = {k: ("CH1" if k[:3] == "CH1" else "RF3" if k[:3] == "RF3" else "CH2") for k in ROIS}
LABELS = list(ROIS.keys())

# 모든 칸이 음수(-) 표시 가능하다고 가정(브리핑 기준).
SIGNED = set(LABELS)

# 각 칸은 항상 숫자 3자리(zero-padded) + 선택적 부호.
N_DIGITS = 3

# 정규화 숫자 크롭 크기 (분류기 입력)
DIGIT_W = 28
DIGIT_H = 40

# CSV 컬럼 순서 (공정 프로그램 출력과 동일)
CSV_COLS = ["timestamp", "CH1_FWD", "CH1_REF", "CH1_LOAD", "CH1_TUNE",
            "RF3_LOAD", "RF3_TUNE", "CH2_FWD", "CH2_REF", "CH2_LOAD", "CH2_TUNE"]
